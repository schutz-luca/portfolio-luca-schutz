import { AnimatedSection } from '@/src/components/motion';

export const Contact = () => {
    return (
        <AnimatedSection id="about">
            <h2>get in touch</h2>
        </AnimatedSection>
    );
};